# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/) 格式（自 0.1.0 起）。

## [未发布]

### 新增

- 项目文件 v2、生成会话状态机、revision-CAS 云端同步与可恢复冲突副本
- 精确 Oklab 色彩匹配、背景原型手动取样、可取消持久 Worker 与算法 oracle/属性/性能测试
- 响应式统一页头、键盘画布、Modal 焦点循环和 WCAG 2.2 AA 自动化走查
- PostgreSQL 16 契约、原子备份恢复演练、迁移优先发布和连续稳定性门禁

### 变更

- 升级到 Next.js 16、ESLint CLI 与请求级 nonce CSP；应用采用动态渲染
- 本地与云端保存状态分离；列表改为游标分页，删除使用轻量墓碑
- 大图裁剪直接收敛到工作分辨率，源图通过 Worker transfer list 传输

### 修复

- 修复生成参数静默失效、错误颜色映射、取消残留笔迹与重新生成覆盖手工编辑
- 修复认证令牌并发消费、会话绝对期限、邮件配额和统一 API 错误契约
- 修复 PNG/PDF 极限导出、HEIC 探测、比例锁、备份假成功与发布绕过门禁

## [0.2.0] - 2026-08-16

全站优化版（12 张工单：视觉/配置/安全/体验/SEO/工程健康）。

### 新增

- 全站「温柔治愈」视觉升级：奶油白底 + 主粉 + 墨紫 + 丁香紫设计系统（CSS 变量统一管理）、圆润字体、柔和粉色阴影；登录/注册/找回/重置/验证页重设计；移动端 ≤640px 展示修复；OpenGraph 分享图（og.png）
- 站点配置化：生成/导出默认参数与安全阈值改为环境变量下发（改 .env 重启即生效，无需发版），经 /api/config 注入客户端
- 生成 Web Worker 化：大图生成不冻结页面，显示进度（>300ms 才出现），支持取消；Worker 异常自动回退主线程
- HEIC 照片 WASM 兜底：Chrome/Firefox/Edge 可上传 iPhone HEIC（Safari 原生支持不变）
- 工作台接入云端自定义色板（登录后可选「我的色板」）
- 编辑器新增镜像翻转（左右/上下）与 90° 旋转（可撤销）
- PNG 导出选项面板：格子大小、图例开关、裁剪至内容开关
- 备份失败邮件告警（内部令牌鉴权端点 + backup 容器自动通知；未配置令牌时仅记日志）
- SEO：robots.txt、sitemap.xml、OpenGraph/Twitter 分享卡片、移除残留占位资源

### 安全

- 升级 Next.js 15.5.23 / React 19.2.8（修复 RSC RCE CVE-2025-66478 等）
- 攻击者视角自查修复：mutating 请求 Content-Type 守卫、速率限制、备份容器密钥面收敛、CSP 加固、登录失败统一文案等
- 修复 Firefox 下 worker 取消崩溃页面问题（改为丢弃语义）

### 工程

- 覆盖率护栏：CI 强制 src/lib 行/语句/函数 ≥90%、分支 ≥75%（实测行 92.9%），新增 40+ 单测
- 生成性能阈值收紧到 spec ≤2s；编辑器核心操作性能护栏
- 依赖评估升级 + 日志清理 + 魔法数字收拢

## [0.1.0] - 2026-08-15

首个完整版本。

### 新增

- 上传照片/像素画生成拼豆图纸（裁剪 → 调参 → 生成 → 编辑 → 导出）
- 内置 5 套国产色板：漫德 MARD / COCO / 漫漫 / 盼盼 / 咪小窝（各 291 色），支持自定义色板（500 色上限）
- 生成参数：目标宽度（20–200）、颜色数量（2–128）、抖动、主色/平均色取样、背景去除
- 像素编辑器：画笔/橡皮、吸色、填充替换、撤销重做、方向键微调
- 导出：PNG 图纸、打印版 PDF（A4 分页 + 29×29 板缝线 + 图例与色号用量清单）、项目文件（JSON）
- 账号体系：注册、邮箱验证、登录、找回/重置密码、修改密码、注销（密码校验 + 级联删除）
- 云端同步：设计随账号多设备同步（LWW 冲突解决 + 客户端时钟偏差防护）
- 安全：argon2id 密码哈希、会话令牌哈希存储、Origin 守卫、接口限流、CSP、请求体大小上限
- 测试：435+ 例单元/组件/API 测试 + 39 例三浏览器 E2E（含真实拖拽裁剪回归）

### 变更

- 授权协议采用 AGPL-3.0（基于 Zippland/perler-beads 二次开发，致谢见 NOTICE.md）
