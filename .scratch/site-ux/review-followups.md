# 阶段 01–07 双轴审查跟进

固定点 `7a345e1`，审查截止 `fe3a7f5`。阶段08仍在实施，不表示最终交付通过。

## Spec

- 重绑原图时空白起稿可能覆盖旧设计：已隐藏重绑页面的空白入口并增加内部 guard；75 项 Workbench 回归通过。
- 首页豆社区块吞掉错误/空态：已增加加载、失败重试和真实空态；行为测试通过。
- 首页跟拼进度读取失败猜成无进度：已改为失败闭环与重试；行为测试通过。

## Standards

- 编码点段规范化造成开放回跳：已在 URL 规范化后再校验单斜杠，增加同目录纯逻辑测试及入口回归。
- 跨标签页旧 grant 覆盖撤回：已串行化请求并禁止旧 grant 写回可读同意 Cookie；初始化/无锁环境 fail-closed；Chromium/Firefox/WebKit 三浏览器本地 HTTPS 阻塞 grant→withdraw 用例全部通过。
- grant 丢响应后的无关联空哈希：经原始 raw/links 契约复核不构成采集/隐私违反，已补一天后保守维护清理及 ADR 边界；PGlite 测试通过。
- 来源版本仍可省略：API/service 已改为必填；漏传 400、过期 409，PGlite 投稿/领域 27 项组合回归及 PostgreSQL 并发合约 10 项通过。
- 新增界面文案硬编码：已集中迁移 zh-CN；TypeScript AST 扫描本次全部改动的页面/组件，未发现残留中文 StringLiteral/模板/JSX 文案（不扫描注释和测试）。12 文件 165 项行为回归通过；读取回调稳定化，不添加 lint 忽略。
- 票据完成状态与 triage 字段混用：已统一 canonical Status + 独立 Completion 标记，并补跟踪约定。
- 手机直接打开跟拼的模态焦点：复用 Portal Modal、背景 inert、初始焦点与显式 Tab 循环；81 项工作台/Modal 回归和三浏览器本地 HTTPS 直接恢复/35 次键盘循环/退出均通过。Safari 跳过按钮的键盘设置边界已修复。

## 阶段08新实施

- 共享后台读取错误/重复提交/不确定结果同 key 重试、选中材料聚焦与返回焦点已实现；列表请求带序号隔离，切换过滤后不显示上一查询的数据。
- 审核页加载完整冻结图纸及旧公开版对照；评论/举报先展示当前案件材料；标签用具名目标与二次确认合并；人员先选账号再理由/完整 userId 确认；规则复制当前整表编辑，显式确认替换，不允许空词表和同分类重复词。
- 补齐作品管理 GET 列表/详情、50 条稳定游标分页、后台页面/导航；下架/恢复再次确认，无批准记录不能恢复，业务计数来自事实表。作品 active 不再误标为“公开中”，正常生命周期与公开可见分开显示。
- 12 文件 27 后台/Modal/API 测试、PostgreSQL 16 合约 10 项通过；七模块队列/详情 × 五种宽度 70 个 Chromium 场景无溢出、axe serious/critical 为 0，已目视。初轮发现导航编号对比度、规则返回焦点、作品表单网格撑宽与预览 checkbox 被通用样式放大，均已修复并重跑。
- 分析/审计/系统只读任务已补齐；官方批次、迁移执行时间新证据、完整门禁及最终双轴审查未完成。

## 08A 双轴复审与 08B 只读任务

Spec：注销前冻结昵称已从管理搜索排除；评论举报增加对已核查当前版本的隐藏入口，案件受理/结案不冒充内容下架；作品举报可以定位到具名管理材料。标签重复名称/slug 映射确定的 STATE_CONFLICT，创建使用 expectedVersion=0，排序校验 int4，更新冲突可修改重试。同态精选/评论公开不增版本和审计。

Standards：治理列表/详情 private,no-store，举报 DTO 不发送举报人/处置者内部 ID；500×80 合法 Unicode 词表可在有界 512 KiB 下保存；HTTP 408 与 15 秒写超时保留同 key/body，列表与详情读取超时可重试。管理员恢复已批准的 withdrawn 作品是原计划已授权语义，不擅自禁用；评论历史正文未持久化，版本变化仍明确显示，不新增隐私保留范围。

实际浏览器发现规则长表单的 sticky 区覆盖后续冲突恢复按钮；已移除该两列长表单吸顶（审核三栏的处置区仍独立），三浏览器完整实写旅程通过。首次 Firefox/WebKit 读取页测试在重置导航完成前启动下一次 goto，已等待 URL 与无效提示消失再继续；不把测试竞态当产品成功。

本轮阶段证据：16 文件 36 项 UI/读取/API/查询测试 + 4 文件 25 项领域/规则测试、PostgreSQL 16 11 项、耐久 E2E 18 项通过；五宽度只读视觉 25 场景通过。全量稳定门禁仍待最后执行。

全量门禁前自查已修：普通投稿 HTTP 408 保留冻结内容/同键；投稿和我的投稿的成功结果校验目标、状态与版本，15 秒请求超时按未知结果处理。我的投稿未知结果禁止关闭确认/切换动作，重试不随新 props 改变目标，刷新失败不重放成功写入。29 项单元及三浏览器真实提交后丢响应恢复通过，见 verification 08F。

## 最终双轴复核（7a345e1 → 08G 工作树）

### Standards

未解决硬要求 0，判断建议 0。批次/后台读取/社区写入不透传浏览器、解码器或 Worker 原始错误；校验文案集中。未发现权限、隐私、并发、迁移证据或资源生命周期退化。

### Spec

未解决 0。复核关闭此前 8 组问题：批次严格成功集合与历史 DTO、投稿 UUID/版本确认、同意失败横幅、长期当天数据与逐日分类、730 天最老完整日、六个主内容锚点、首页精选和最新同时展示。不存在未经授权的语义扩展。

两名独立只读审查员均确认关闭；最终全量门禁当时仍在执行，不将其写成代码缺陷或通过证据。Safari 链接遍历按官方测试使用 Option+Tab；Secure Cookie 的三浏览器失败恢复由真实回环 HTTPS 代理验证，不降低 Cookie 安全属性。代理转发 Next dev HMR 握手以保证真实 hydration；证书和连接在测试结束后清理。

## 08H 性能修正独立复核（585ed0b → acebeca）

Standards 未解决硬要求 0、判断建议 0；Spec 未解决 0。两位只读审查员分别确认，相同尺寸预览快路保持逐字节值、独立缓冲、精确长度、长源截断及短源补零；缩放、自然坐标、原图、提交和撤销路径未改变。单元覆盖长度边界、访问预算及源数据不可变；性能阈值未放宽。最终门禁仍以之后的完整运行结果为准。

## 08J 测试契约复核

Standards 0、Spec 0。手机导航五入口的名称/可见性/href 比旧两标签检查更完整；用色采购清单只在当前工具分支渲染，精确总量、逐色列表和复制按钮不会被隐藏桌面内容误满足。保留原无溢出及工具切换断言，无 skip/timeout/retry/性能门槛修改。两位审查员确认未削弱验收；完整 06 三浏览器 56 项通过、10 项原设计跳过。

## 08K 可中断裁剪打开复核

Standards 0、Spec 0。两位审查员确认，`startTransition` 只降低打开裁剪视图的更新优先级；焦点同步，所有图片操作及操作令牌不变。同一 state 队列保持更新顺序，后发急更新不会被旧 crop 覆盖；清源同时清除 decoded，不能挂载旧 CropDialog。确认/取消、手工编辑覆盖、提交、撤销与路由卸载保护不变。60 项无采样前置流程通过；原 51 ms 失败仍保留，最终性能与完整稳定门禁需后续通过才关闭。

## 08L 顺序夹具与按需缺原图测试复核

Standards 0、Spec 0。两位独立审查员确认：社区测试唯一选择具名种子作品，并在负权限断言前确认目标评论存在，不再被前一浏览器新发布的其他作品干扰；缺原图测试验证计划约定的按需帮助，而不是旧 disabled 控件。保留真实裁剪、取消/确认、图纸粒数及拒绝替换保护，没有降低验收。

扩展回归随后发现 Firefox 偶发的取消后入口焦点恢复错误；此前静态审查不构成实际焦点稳定证据。已定位为 Modal 在背景 inert 之后的被动 effect 才读取原焦点，新增三项先红回归并修复。该产品修复另行双轴复审，不以测试契约审查替代。

### Modal 修复独立复审

Standards：硬违规 0、判断建议 0。Spec：未解决 0。两位审查员分别检查 effect 声明顺序、StrictMode 清理/重连及逐层嵌套：先挂载不抢焦点的 portal，再于 inert 前捕获入口；关闭时 layout cleanup 先恢复背景，被动 cleanup 再聚焦入口。稳定 portalRoot 的普通 rerender 不覆盖入口引用。未改变 Escape 顶层独占、Tab 循环、背景快照、裁剪取消/确认及图像状态；新增先红测试与三浏览器各五次的实际焦点回归支持修复，最终完整稳定门禁仍另行验证。

## 08N 最大批次确认清单独立复核

Standards：硬违规 0、判断建议 0。Spec：未解决 0。两轴独立确认：批次面板高度配合外层 padding 保持动态视口内上下 16px，内部滚动保留首尾可达，inspection 仍为独立较宽预览；复选框仍受原 confirmed 控制，取消不发布、不清选择，重新打开明确重置确认。新增真实 50 项先红回归覆盖原一项视觉用例未覆盖的长清单边界。

WebKit 原生指针点击不聚焦按钮导致新增返回焦点断言失败，入口同步 focus 行另经两轴复核：在 Portal 挂载/inert 前冻结入口，键盘情况下幂等；不改变 disabled、selected、许可、发布或幂等路径。最终 15 文件三浏览器 10 项通过、2 原设计跳过。没有隐藏失败或降低检查；此后完整门禁重新执行。

## 最终闭环 — 44f973d

Standards 未解决 0，Spec 未解决 0，均无最严重遗留项。最终产品源代码已完成全部本地门禁：185 文件 1460 项测试与覆盖率、五轮性能、真实 PostgreSQL 合约/迁移演练、生产镜像与本地运行；完整三浏览器连续三轮，每轮 218 passed、22 原设计 skipped、0 failed。包含新增 50 项长清单、取消后焦点和所有前述故障恢复回归。之后只更新证据文档与截图，不修改产品逻辑。

本结论为约定本地验收范围内无已知未关闭发现，不替代专业法律、真机或生产验证；原失败及修复过程完整保留于 verification.md。
