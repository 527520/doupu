# 整站任务优化验证记录

## 01 首页与导航 — 2026-09-05

- RED→GREEN：缺少手机次级入口；最近记录/进度入口；显式 edit/stitch URL 恢复；损坏记录不误报空库。
- 定向 4 文件 79 测试通过；typecheck、lint、diff --check 通过。
- capture-home.mjs：本地 dev + 隔离 PGlite，真实图片生成与 IndexedDB 保存，350/390/768/1280/1440 px 均无横向溢出、axe serious/critical 为 0；已目视截图。
- 最近设计→指定图纸编辑模式：1 次点击，不含此前图纸创建/文件选择。继续跟拼由已有兼容进度推导，单元覆盖 50% 进度。
- 选图为主页主动作；空白起稿保留不同创作起点；全部设计保留全量访问；豆社提供公开作品；教程按需展开；更多入口提供手机色板/帮助/隐私。账号资料与退出在账号页管理。
- 测试公开作品为明确命名的启动期种子 E2E 已公开作品，不是产品示例/推荐数据。截图中的开发指示器属于本地 Next dev，不是交付 UI。
- 本阶段不代表三浏览器、真机或生产验证；最终全量门禁在阶段 08 执行。

## 02 设计管理与继续制作 — 2026-09-05

- RED→GREEN：卡片 edit/stitch 直达；本机读取失败、损坏记录与初始列表读取失败；菜单弹窗关闭焦点恢复。增加下载失败重试、进度读取失败、连续点击和删除失败保留数据测试。
- 定向 4 文件 94 测试通过（设计列表 19 项）；typecheck、lint、diff --check 通过。未更改同步 CAS、墓碑和跨设备删除语义。
- capture-home.mjs 扩展至设计页：五种宽度无溢出、axe serious/critical 为 0；实际图纸卡片 1 次点击→指定编辑模式，关闭重命名后焦点回可见管理按钮。
- 目视发现 350px 登录链接被挤成逐字竖排，已改为段落内不可断开的链接并复验；768px 的页面标题也已恢复。
- 删除、重命名和投稿移入每张图纸的管理入口；保留删除确认、请求中禁用、失败在当前弹窗展示及再次确认。创建新图纸仅一个主入口。
- 三浏览器完整跨设备删除 E2E 在阶段 08 复跑；此处浏览器证据仍是本地 Chromium + PGlite，非真机/生产。

## 03 工作台 — 2026-09-05

- RED→GREEN：idle 不伪报已保存；dirty 不沿用旧云端成功；手机配额失败不显示成功；缺完整原图的裁剪引导按需出现；指定记录缺失不误开其他图纸；可选生成源读取失败仍可编辑且保存不清源。
- 定向 4 文件 109 测试通过，typecheck、lint 通过；原自动首版/裁剪/手工覆盖确认/取消回滚/原子撤销与同步冲突测试保留。
- capture-workbench.mjs：真实选图自动生成/自动保存；五宽度预览及 350/390 编辑和跟拼共 9 个场景无横向溢出、axe serious/critical 为 0；已目视关键截图。
- axe 发现结果句淡入瞬间对比度不足，移除状态文字淡入；发现桌面用色滚动区键盘不可达，增加聚焦入口，均复验通过。
- 参数保留尺寸、色数、色板/规格/套装；抖动归入默认收起的高级选项。制作步骤在进入工作区后只保留读屏语义。保存按钮保留立即落盘/失败重试用途，自动保存无需点击。
- 缺原图不会阻止修补和跟拼；仅点击裁剪后出现重选说明。无生成源时调整参数区域提供具体原因和重选入口，不再在手机预览前放恢复警告。
- 沉浸工作区固定定位；full-page 截图下方留白为被遮罩背景的文档长度，不代表可滚动的额外画布。真实触控/软键盘及三浏览器全量验证仍待 08。

## 04 豆社浏览与互动 — 2026-09-05

- RED→GREEN：所有筛选随游标保留；无结果清除筛选；非法日期恢复入口；真实点赞切换；一次引用→落本机→准确独立设计；不确定响应复用幂等键；已创建但下载失败仅重试打开；401 返回原作品；举报先确认。
- 增补：评论加载失败保留输入、服务端新评论锁定禁止重复提交、离开后的迟到引用不强行导航、请求去重；每个作品组件按 ID 隔离生命周期。
- 4 文件 27 单元/PGlite 测试通过，typecheck、lint、diff --check 通过。新增 GET like 响应仅 liked/likeCount，private/no-store + Vary Cookie，不返回内部用户 ID。
- 新建本地隔离 PostgreSQL 16 容器 doupu-siteux-pg（仅 127.0.0.1:55442、tmpfs、无宿主数据挂载），完整迁移成功；8 项治理/并发合约通过，含新增点赞读取的用户隔离断言。未访问生产数据库。
- capture-community.mjs：真实登录、点赞/取消与刷新一致、引用后打开可编辑的独立设计且 communityOrigin=true；列表、详情各五宽度及手机举报弹窗，共 11 场景无溢出、axe serious/critical 为 0；筛选返回和空态重置通过。复制主流程为 1 次点击，不含登录。
- 图片后即出现“用这张制作”，规格与许可在其下方；版本信息按需展开；报告仅在次级菜单，分类确认后才提交。评论提交是次级按钮。
- 浏览器脚本曾过早读取尚未完成导航的旧 URL，已增加 URL 完成断言后复验；未以此误判产品返回功能。
- 最终三浏览器和权限/隐私整体验证仍由阶段 08 执行。

## 05 投稿与我的投稿 — 2026-09-05

- RED→GREEN：可信云端选源、标题/作者/真实图纸预览、许可默认不选；不属于当前账号/未同步/已失效的来源不悄悄替换。源版本已变时拒绝冻结，重新确认。
- 5 文件 22 单元/PGlite 测试通过，typecheck、lint、diff --check 通过；创建/新修订/提交/两种撤回支持幂等请求，不确定响应保持原请求，草稿已创建则只重试提交。
- PostgreSQL 16 治理合约 9 项通过，新增 4 路并发创建和提交只产生一个作品、一次状态转移；幂等记录仅保存安全响应，不保存快照或私人正文。
- 我的投稿最新状态优先：草稿可提交或撤回后修改；待审可单独撤回；已发布/退回/已撤回修订可沿原作品身份重新投稿；历史折叠。整件撤回明确告知原公开版和待审版均隐藏，失败仍在确认框中可重试。
- capture-submission.mjs：云端选源→许可→模拟提交失败→原草稿重试→我的投稿→单次撤回→修改重提；五宽投稿与我的投稿、350px 撤回确认，共 11 场景无横向溢出、axe serious/critical 为 0，关键手机截图已目视。
- 首次浏览器脚本误把 Next 路由播报器一起当成业务 alert；已限定表单 alert 并全流程复验。服务端成功保存的测试草稿未冒充失败删除。
- 三浏览器重复 E2E 已加入投稿失败恢复与撤回重提；完整执行留在阶段 08。未改变公开许可、权限、原图或生产边界。

## 06 材料、导出与色板 — 2026-09-05

- RED→GREEN：默认 PNG 一次点击下载，特殊选项保留；材料模式完整色号/包数与一次复制，剪贴板失败提供可选中文本，无效每包数量解释并禁用复制；项目下载失败不再抛出未处理异常。
- 桌面工作台增加与手机一致的参数/用色/导出分区；移除重复且截断的颜色清单。下载进行中有重复点击保护，PDF/项目 URL 延迟释放，PDF 明示页数、实际每格 mm 与 100% 打印要求。
- 色板库用真实色带与规格，来源说明默认折叠；去除装饰扇形与暗示库存管理的文案。从工作台带指定 ID 进入，返回后确认应用，仅对该图纸重映射，规格与图纸一起撤销；没有明确 ID 不应用到最近图纸。
- 自定义色板保存/版本冲突错误在当前弹窗可见并保留输入；会话失效另开窗口登录后重试，不自动丢弃编辑。保存中保护控件与关闭，删除重复点击保护，取消后可重试。
- 6 文件 121 单元测试、typecheck、lint、diff --check 通过；包含原裁剪、同步、手工修补与跨设备结果回滚的工作台测试。无本阶段服务端数据模型变更。
- capture-materials-exports.mjs：材料/导出/筛选色板库各五宽，共 15 场景无横向溢出且 axe serious/critical 为 0；真实 PNG/PDF/项目下载内容验证通过（PNG 文件头、%PDF、项目 v3 且无原图源字段）。PNG 1 次、PDF 2 次、项目文件 1 次点击，不含打开导出分区。
- 浏览器发现手机切换导出后首个下载按钮仍在首屏底栏之下，已在切换工具时滚动到可操作区域，并新增边界断言确认按钮在底栏上方；图纸级换色返回原 ID、Mini 规格自动兼容与一步撤销恢复完整原图纸/色板均通过。
- 截图已目视；此处是本地 Chromium、模拟剪贴板拒绝与浏览器下载，不是纸质打印、真机或生产验证。最终多浏览器回归在阶段 08。

## 07 账号、认证与公共页面 — 2026-09-05

- 安全返回路径在登录/注册/找回密码间保留；拒绝跨源、编码分隔符、控制字符与超长路径；不自动重放投稿或同意。未修改邮件服务契约，邮件新窗口无返回参数时仍回设计页。
- 账号读取失败明确显示重试，不冒充游客；退出失败不触发成功状态；重复提交被阻止，修改密码/注销请求期间禁止关闭确认框。注销说明与实际匿名化保留语义一致。
- 分析撤回先立即写现有偏好 Cookie 的 withdrawn 待清除标记并清空内存队列；服务器不把该标记当同意。失败/刷新后持续停采并提供清除重试，确认清除前不能重新同意。不新增访客身份、原图或持久事件队列。
- 页内横幅/设置共享选择和请求锁；事件发送前去除查询串、分享 token 和 referrer 路径；已清空的在途旧批次不因重新同意而复活重试。
- 手机恢复可见页标题；公共说明与表单正文增大，隐私设置靠前，FAQ 按需展开，真实只读分享规格/用色可读，主入口明确新建。移除认证和说明页的装饰色块，不改变分享轮换/权限/法律实义。
- RED→GREEN：15 文件 66 项单元/PGlite 通过；lint/typecheck/diff --check 通过。capture-secondary.mjs 共 12 类页面 × 5 宽度 = 60 场景无横向溢出、axe serious/critical 为 0；另验证 350/390 短视口确认框焦点、分享无邮箱、撤回失败→刷新→重试→HttpOnly 访客 Cookie 清除。关键截图已目视。
- 首次扫描发现按钮 disabled→enabled 的 opacity 动画暂时降低对比度，以及分享 dl 的 group role 覆盖了定义列表语义；均修复后完整重跑。证据仅为本地 Chromium 与模拟短视口，不声称真机软键盘、真实邮件或生产验证。

## 01–07 复审修正 — 2026-09-05

- 首页进度读取失败不再猜成无进度；豆社区块有真实加载、失败重试、空态；重绑原图时禁止空白起稿覆盖已恢复设计。
- 编码点段回跳在 URL 规范化后再次校验，避免归一化产生双斜杠跨源路径；新增同目录纯逻辑回归。
- 分析同意/撤回通过同源 Web Locks 串行化到响应处理完成，grant 响应不再写回可读偏好；同意初始化成功前不采集，无协调能力的浏览器保持关闭。撤回先写停采标记，不等待网络。grant 丢响应留下的无事件/无身份关联空哈希一天后维护清除，不将随机空行误称为采集事件。
- check-consent-tabs.mjs：Chromium、Firefox、WebKit 本地 HTTPS 两页阻塞 grant→撤回→释放旧 grant 全部通过，最终停止采集并删除访客 Cookie。HTTPS 是为保留 Secure Cookie 契约；未放宽生产 Cookie 属性。
- 手机沉浸编辑/跟拼复用已有 Portal Modal，背景 inert、初始焦点、Tab 循环、Escape 与滚动恢复统一。Safari 系统键盘设置会跳过按钮，改为显式遍历可见可操作控件；折叠、禁用和负 tabindex 控件不参与循环。
- 工作台/Modal 81 项回归通过；其他恢复相关 26 项及维护/回跳 9 项通过。check-mobile-direct-focus.mjs 在三浏览器直接恢复跟拼、35 次 Tab/Shift+Tab、Escape 与背景/滚动恢复全部通过，axe serious/critical 为 0；新增稳定 E2E 用例留待最终全量门禁。
- docs/agents/issue-tracker.md 分离 canonical triage Status 和 Completion，已完成票据不再使用不存在的 done 标签。此次是复审修正证据，不表示阶段08或最终全量验收完成。

## 08A 治理任务流程 — 2026-09-05

- 七模块：审核、评论、举报、标签、人员、规则和作品管理。均以真实对象为入口，没有默认选中对象；先读取材料后才允许有理由的写入。新后台文案集中于 zh-CN。
- 共享请求控制防重复提交；网络/5xx/无法解析响应保留冻结 body、expectedVersion 和同一幂等键，只能重试原操作；已成功的写入不会因为随后刷新失败而重复执行。只读列表错误与空列表区分，旧异步响应不会覆盖新查询。
- 审核详情只按选中修订读取完整快照，显示原公开版供对照；举报保留被举报版本与当前版本差异说明；标签使用具名目标而非 prompt 输入 ID，合并要再次勾选；人员保留 userId 二次确认、禁自身治理和匿名化账号保护。
- 规则从现行完整词表开始编辑，明确替换语义、重复词/空表保护和未勾选确认。启用请求必带基准版本，在 PostgreSQL 咨询锁内比较版本，避免两管理员旧表覆盖新表；没有扩充政治分类或可输入正则。
- 作品管理新增授权 GET 列表/材料与独立页面，50 条确定性游标、标题/作者/完整作品 ID 查询；列表不带完整图纸或作者内部 ID。精选/评论锁有理由，下架/恢复再次确认，没有已批准修订不可恢复。恢复保留既有审计和冻结语义。
- 投稿源 expectedDesignRevision 在 API/service 强制必填；遗漏 400、变更 409。源版本、投稿幂等和领域事务 27 项组合回归通过。
- 12 文件 27 项后台/Modal/API 定向测试通过，lint/typecheck/diff --check 通过。真实 PostgreSQL 16 合约 10 项通过，新增跨管理员相同基准词表竞争只成功一次，以及作品列表/详情读取隐私断言。
- check-admin-tasks.mjs：七模块 × 队列/详情 × 350/390/768/1280/1440 px = 70 场景，无横向溢出、axe serious/critical 为 0；手机切换模块、Escape 收起、返回原队列焦点通过。目视修正预览 checkbox 被通用表单样式误放大，状态文字也不再把 active 误报为公开。
- 正常审核：选中对象 1 次 + 填理由 + 批准 1 次；评论/举报处置相同。作品下架：选对象 + 填理由 + 下架 + 再勾选确认 + 确认下架；保留的额外点击均用于不可见化影响确认。标签合并保留具名目标选择和迁移确认，人员保留完整 ID 确认，规则保留整表替换确认。
- 本地截图/测试种子、浏览器模拟与 PostgreSQL 合约不是生产或真机证据；08B 官方批次/分析/审计/系统和最终完整门禁仍待完成。

## 08B 只读证据与治理复审 — 2026-09-05

- 分析：常用五字段与按需展开的设备/来源/UTM 筛选分层；无效/重复/过期参数明确恢复默认值；长期查询明确忽略组合筛选。SVG 点按日期比例定位、可聚焦、有文本摘要和可滚动数据表，不伪造长期跨日 UV。
- 审计：按动作/目标 ID/请求 ID、上海日期筛选，50 条稳定游标分页。选择后才展示白名单前后状态、理由与请求；读取失败可重试，不显示假空列表；只读无编辑/删除。
- 系统：应用版本、本地迁移 journal、数据库记录分开；Drizzle created_at 实际来自 journal.when，不能冒称执行时间，历史实际执行时间显示未记录。各任务最新状态与最近成功/失败独立从保留记录查询，不被最近 50 条历史截断；备份仍“未接入”。CLI 新迁移执行时间留痕在最后演练阶段补齐，不倒填历史。
- 复审修复：匿名化昵称反查、举报当前评论隐藏与独立结案、标签碰撞/整数范围、同态动作、规则大词表载荷、最小 DTO/no-store，以及 408/15 秒超时恢复。读取取消/超时也有界，旧请求不能覆盖新目标。
- `/tmp/doupu-siteux-admin-evidence-focused-final.log`：16 文件 36 项通过；`/tmp/doupu-siteux-review-08a-db.log`：4 文件 25 项通过；`/tmp/doupu-siteux-admin-evidence-postgres.log`：真实 PostgreSQL 16 11 项通过。本阶段提交前最终 lint、typecheck 和 diff 检查通过。
- `check-admin-evidence.mjs`：分析折叠/展开、审计列表/详情、系统 × 五宽度 = 25 场景通过，无横向溢出、axe serious/critical 0；包含无效筛选、长期模式、审计读失败/重试、详情返回焦点。目视检查 350 px 审计/系统与 1440 px 分析，随后改善证据字段手机单列、系统留白和图表轴。
- `/tmp/doupu-siteux-admin-tasks-e2e-final.log`：耐久 14-admin-task-workflows 的六条旅程 × 三浏览器 = 18 项通过，1.1 分钟。覆盖真正提交后丢响应同键恢复、标签改名停用合并、人员暂停撤销会话/恢复/角色调整、规则 stale→读取→重新确认、作品评论锁/下架/恢复、审计检索及举报内容隐藏/结案。
- 首轮实写暴露长规则表单吸顶覆盖恢复按钮，已修复后重跑全部 18 项；读取页原测试未等待重置导航完成，已修复测试边界后全部重跑。无 force click、无重试掩盖失败。三浏览器完整稳定全量、生产构建/本地模式和最终审查仍未完成。

## 08C 官方批次任务与中断恢复 — 2026-09-05

- 浏览器内批次会话独立管理派发与冻结写请求；最多两个执行槽，低并发设备一个。暂停不打断已经运行的保存，取消覆盖文件读取完成、解码、区域提取和生成；保存结果不明确时先同键确认再取消服务器批次，不把已提交草稿丢掉。
- 创建、批次状态、发布和逐项保存均有 15 秒有界请求；未知结果仅重发同一 body/key，不重新生成；已明确拒绝的保存可跳过或重试。原图 File 在成功保存后释放，解码与生成 Worker/RGBA 在请求保存前释放；未上传文件名、原图或裁剪源，未增加持久队列。
- 可视裁剪复用已验证裁剪框与自然坐标；逐项标题/参数在启动后冻结。生成后自动收起统一参数，展示实际派生预览，可按需读取完整冻结图纸。发布默认不勾选，列明所选标题后再次确认内容权利与公开影响。
- 历史改为主动选择最近批次，只恢复服务器草稿；本地文件被替换前明确确认，迟到的列表不自动覆盖任务。冲突刷新保留本地文件/未确认请求并清空发布勾选；历史默认参数读取真实批次记录。批次 GET private,no-store。
- `/tmp/doupu-siteux-batch-unit-final.log`：3 文件 22 项通过；`batch-db-final.log`：官方批次 PGlite 合约通过；lint/typecheck/diff 检查通过。启动 E2E 的同时跑过一次 typecheck，捕获 Next 自动生成类型尚未完成的竞态；待启动结束重跑完整 typecheck 已通过，未修改或排除生成类型。
- `/tmp/doupu-siteux-batch-e2e-final.log`：三条实际操作旅程 × 三浏览器 + Chromium 五宽度视觉用例共 10 项通过；另外两个浏览器的重复视觉用例按设计跳过，不计为通过。覆盖部分失败、主动恢复、实际裁剪、创建/保存/发布已提交后丢响应同键恢复、暂停时保存、取消一项后单独重试。
- `15-official-batch-recovery.spec.ts` 准备/裁剪/实际草稿/发布确认 × 五宽度 = 20 场景，无溢出、axe serious/critical 0；350×400 短视口发布按钮可见、Escape 退出。目视后收起冻结参数、修正勾选布局和文件选择焦点环，并单独完整重跑 20 场景（batch-visual-final.log）。截图为本地浏览器，不是真机或生产证据。
- 默认整图批次：选择文件 + 开始生成；可选裁剪额外打开/确认两次。发布：逐项勾选 + 打开清单 + 勾选权利确认 + 确认公开；这些额外确认服务于官方立即公开，不自动发布。最终全量稳定门禁与独立双轴审查尚未完成。

## 08D 迁移执行时间证据 — 2026-09-05

- `db/migrate.cjs` 使用独立连接的咨询锁与有限超时；仅真正应用新迁移时记录 `database.migrate` 开始/完成时间。维护证据与迁移序号、journal 时间和 hash 匹配；系统页只显示当前迁移身份的成功记录，不用历史失败、无关 hash 或 no-op 时间替代。
- 已有缺失时间继续“未记录”；成功迁移后留痕写失败返回独立错误码并明确不代表迁移回滚。CLI 不输出原始数据库错误和连接串，连接释放时销毁以避免遗留锁。ADR-0005 记录这些证据边界。
- PGlite 系统查询 4 项通过（错误 hash/失败记录不采纳、匹配成功记录可显示）；真实 PostgreSQL 16 在本任务临时实例的新 `siteux_upgrade_evidence` 数据库，从带用户/设计/分享的 0004 升级，验证实际时间、no-op 不重写、配套 down/重升、咨询锁竞争拒绝、故意失败 DDL 的事务回滚与稳定失败记录，全部通过。另直接运行 CLI no-op 返回预期提示。
- 未新增迁移、未改动生产，也未声称在生产执行或验证。静态检查已通过；最终全量门禁仍待完成。

## 08E 文案归一 — 2026-09-05

- 投稿/我的投稿、账号、色板返回与换色、材料/导出提示及最近任务辅助名称集中引用 zh-CN，保留原措辞与行为。TypeScript AST 扫描本次改动的所有非测试页面/组件，中文 StringLiteral/模板/JSX 文案残留为 0；注释与测试不属于界面文案。
- 12 文件 165 项回归通过，typecheck 通过。文案引用暴露投稿读取 effect 的依赖警告，已将选择来源回调稳定化并列出依赖，不添加忽略规则；提交前再跑 lint 与投稿定向回归。
- 最终门禁前仍需补普通投稿/我的投稿的未知响应保护（详见 review-followups），然后执行全量稳定门禁与双轴复审。

## 08F 普通投稿不确定结果 — 2026-09-05

- RED 两项：创建 HTTP 408 错误解锁、撤回空成功响应被当作成功。增加社区写入的 15 秒有界传输和确定拒绝分类；创建/提交/撤回确认正确目标、状态及版本后才更新界面。
- 未知结果保留原 URL/body/key/expectedVersion，禁止切换撤回范围与关闭确认框；新 props 不改变冻结请求。确定 4xx（除 408/429）可解锁，已确认的写入不因刷新失败重放。
- `/tmp/doupu-siteux-community-unknown-green.log`：3 文件 29 项通过；lint/typecheck/diff 检查通过。耐久 12-community-governance 在真实创建后 408、真实提交后 503、真实撤回后空 200 下验证同键恢复，Chromium/Firefox/WebKit 3 项全部通过（community-unknown-e2e.log）。全量最终门禁仍在执行。

## 最终门禁中发现的旧测试入口 — 2026-09-05

- 更新测试按真实任务入口展开高级设置与导出工具，默认 PNG 一次下载、PDF/特殊 PNG 明确作用域；保留透明像素、500 色 PDF、拼板 ZIP、生成取消/恢复及长任务限制，不放宽阈值。
- 首页状态按认证加载语义选择，紧凑首页不再断言旧三步说明卡。`gate-test-alignment.log` 两文件 9 项通过；`gate-export-isolated.log` 极限上传/导出 9 项通过。此前与重型任务并行时出现一次 53 ms 长任务，作为失败记录保留，隔离复验未修改性能判据。
- 首次全量 E2E 在旧选择器失败后终止，不计成功；最终稳定三轮需在后续代码冻结后完整重跑。

## 08G 最终双轴发现修正 — 进行中

- 批次 create/save/transition/publish 精确验证 ID、状态、递增版本和所选修订集合；不完整 2xx 保留原请求与本地字节。历史列表/手动刷新/恢复入口校验日期、计数、参数、合法修订和预览，损坏历史不能替换本地文件。
- 解码/像素/Worker/网络/JSON 错误映射中文受控文案；同意初始化失败横幅保持可见且允许重试，未初始化仍不采集；投稿创建 UUID 校验后才推进提交。
- 六个后台主内容锚点补齐。首页分别请求精选和最新，两个区块同时显示，零精选不伪造精选；读取 15 秒有界、损坏 DTO 进入重试错误。
- 长期报表保留已去标识的历史日聚合，仅追加上海当天原始事件，排除任何当天旧聚合避免重复；逐日单维点与可选择分类的图表/数据表可用，跨日 UV 仍为空。明确当天未结束、历史以维护水位为准。查询/UI/维护共享 730 天上海日边界，包含最老仍保留的完整一天及闰年情形。
- 定向 `review-recovery-green.log` 9 文件 83 项；`review-final-targeted.log` 6 文件 49 项通过。真实 PostgreSQL 16 `review-pg.log` 12 项通过；`final-query-plans.log` 14 条实际应用 SELECT 完成 EXPLAIN ANALYZE（小数据夹具，不代表生产规模性能）。最终完整门禁尚未完成。
- `release-recovery-browser.log`：后台六页键盘链接、HTTPS 同意失败恢复、损坏历史保护三项 × 三浏览器，加首页五宽度视觉，共 10 项通过；2 个重复视觉用例按设计跳过。之前 HTTP/Safari Secure Cookie 和 macOS form-only Tab 的测试环境不匹配分别按真实 HTTPS/Option+Tab 修正，不改产品安全边界。新首页两张代表截图已目视。
- `release-coverage.log`：185 文件全部通过，1454 项通过、13 项按原设计跳过；Statements 91.04%、Branches 82.85%、Functions 95.53%、Lines 94.61%。随后仅类型收窄与测试代理/键盘环境修正，无业务行为变化。静态门禁仍在最终复验；完整 E2E 稳定三轮、性能与本地生产运行仍待完成。

## 08H 大图裁剪打开的长任务 — 2026-09-05

- 稳定 E2E 第一轮在 64 MP 场景记录 52 ms 长任务，立即停止该轮，不记为通过。此前隔离重跑 10 次无失败，保留注册/导出/HEIC 前置旅程再抓 CPU profile 后 3 轮中复现 2 次（57/56 ms）。`large-image-profile.log` 保留结果。
- 采样定位为同步裁剪挂载：Workbench/子组件开发渲染和预览处理在同一任务内，buildCropPreview 的相同尺寸逐像素复制占约 10.8 ms；该窗口未显示 GC 为主因。原解码器已生成最长边 800px 预览，800→800 再采样没有改变像素。
- 仅相同尺寸增加原生 typed-array copy 快路；保持独立缓冲、精确目标长度、越界源截断与不足通道补零，不改比例/自然坐标/原图/提交/撤销语义。操作预算单元先 RED（逐像素访问 2,560,000 次），修复后通过；50 项裁剪单元通过。
- `crop-copy-browser.log`：同样的 00–03 前置旅程无 profiling 连续三轮共 36 项通过，原 50 ms Long Task 判据未修改。临时 CPU profiling 分支已移除。该修正后需重新运行最终覆盖率、构建与稳定 E2E。
