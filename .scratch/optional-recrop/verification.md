# 整图首版与可选裁剪：本地验收记录

状态：本地验收通过，Spec / Standards 审查均无未解决发现。日期：2026-09-05。基线：`40787e3`；不 push、不 deploy、不访问生产。

## 实现与审查

- 整图自动首版；预览旁裁剪；确认自动更新；取消/拒绝覆盖保留手工修补。
- 完整原图仅在当前浏览器解码会话；清理后迟到任务失效。持久化仍只有 ≤800×800 本地生成源。
- 裁剪源与图纸在成功、失败、取消、撤销、无源恢复及冲突副本中保持匹配。
- 移动裁剪随 visual viewport、安全区和横竖屏调整，44px 按钮、比例锁定键盘操作、多指取消、嵌套确认焦点；弹窗高于底部导航。
- 自动整图首版不冒记 `crop_completed`；后台原漏斗明确标为包含可选裁剪的路径。
- Spec / Standards 两条独立审查已复核闭合。另经截图检查修正底部导航视觉遮挡。

## 最终门禁

| 门禁 | 结果 | 证据范围 |
| --- | --- | --- |
| `npm run typecheck` / `npm run lint` / `git diff --check` | 全部通过 | 当前最终源码 |
| `npm run test:coverage` | 155 文件通过；1249 项通过、13 项跳过 | 主机 Node 22.22.3；单元、组件、串行及 PGlite 集成；跳过为既有 Linux-only shell 用例 |
| `npm run test:e2e:stable` | 连续 3 轮，各 174 通过、18 跳过；无重试 | Chromium / Firefox / WebKit；跳过为既有浏览器分工；本轮新增 4 个裁剪场景全部三浏览器执行 |
| `npm run test:performance:stable` | 连续 5 轮，各 4 文件 / 7 项通过 | 本机独立运行，未与构建或覆盖率争用资源 |
| 最终 Docker 构建 | 通过，66 个静态页面 | Node 20 Alpine / Next.js 16.3.1；包含最终文案 |
| 最终候选生产模式冒烟 | 1 项通过 | 本机容器，RSC 导航、CSP、真实生成 Worker、整图首版与再裁剪更新 |
| 最终候选 WebKit 手机截图 | 350/390 px 均通过 | 本地 TLS、触屏模拟；首版 6300 粒 → 方形裁剪 10000 粒，未同意分析时图片流程 API 写请求为 0 |

覆盖率：语句 **90.37%**、分支 **81.82%**、函数 **94.79%**、行 **94.07%**。沿用原门槛 90/75/90/90，不调整排除范围或测试超时。

新增回归覆盖：取消和拒绝覆盖保留手工修补；失败/取消/撤销后图纸与源一起原子保存；撤销后刷新、再次改变生成宽度仍使用匹配的旧源；无源恢复和冲突副本正确清源；迟到解码无效并关闭位图；所有生成失败释放原图；嵌套确认焦点、键盘比例、44px 控件、导航层级、横屏与收缩视口、无严重/致命 axe 发现。原生 CDP 单指拖动、多指取消与页面缩放仅在 Chromium 验证，不冒称 WebKit 原生手势实机证据。

早期一次全量运行发现旧三步断言，已随新两步流程修正。另一次与构建并行的覆盖率运行出现 PDF/邮件限流超时并被停止，未计为通过；最终独立运行 221.59 秒全部通过。未以跳过或降低门槛处理失败。

最终候选镜像：`doupu-app:optional-recrop`，ID `sha256:3c31a0827e8f339e3e51adf4416d0d7d7d7d12595ff982d0a033f8559d6f5c0dab`。本轮专用 PostgreSQL 16 空库迁移已通过；未修改任何迁移或服务端协议。

本机原始日志（临时证据，不依赖其永久保留）：`/tmp/doupu-recrop-coverage-isolated.log`、`/tmp/doupu-recrop-e2e-stable.log`、`/tmp/doupu-recrop-performance.log`、`/tmp/doupu-recrop-build-final.log`、`/tmp/doupu-recrop-production-final.log`、`/tmp/doupu-recrop-capture-final.log`。

## 视觉证据

- [350px](crop-350.png)
- [390px](crop-390.png)

截图来自本机 Docker 生产模式候选，经仅监听 loopback 的自签名 TLS fixture；不是生产部署或真机。测试浏览器仅为该 fixture 忽略证书信任错误，未修改 CSP。

已阅读项目外 8 视图设计稿及三张截图；聊天接口不支持读取该临时侧会话，未声称读过聊天全文。本轮点击数、保留按钮用途及未实施范围见 [spec.md](spec.md)。整套原型视觉和其他业务 UX 建议不属于本次实现。

## 明确边界

本次未新增迁移或改变 ProjectFile/云同步/数据库协议。本地空库迁移不能冒充生产备份、生产升级演练、真实邮件投递或真机验收。此前治理计划的法律审核和部署前置阻断仍保留。本次不读取、修改或提交 `docs/marketing/`。

本轮专用应用/数据库容器、临时数据库卷、Docker 网络、TLS 代理与自签名密钥均已清理；仅删除可重建的本地测试数据，无用户或生产数据。最终候选镜像和仓库截图保留。通过测试与审查表示目前无已知未解决发现，不等于对所有环境作绝对无缺陷保证。
