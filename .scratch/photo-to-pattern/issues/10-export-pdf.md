# 10: 打印版 PDF 导出

- Status: resolved

## 完成记录

- `src/lib/export/pdfLayout.ts`（纯布局，全部可单测）：分页（31 列×45 行，6mm/格，A4 纵向 10mm 边距+页眉带；页数 = ceil(W/31)×ceil(H/45)+1，200×200 → 35 图纸页+1 图例页=36 页，与票内「≈35 页」的近似说法一致、以 E2E 验收公式为准）、页眉文本（第 x/y 页·列/行 1-based 闭区间）、页内板缝位置、文本宽度估算（CJK 1em/ASCII 0.55em）与超宽截断、WinAnsi 安全化（Helvetica 限制；升级路径已注释：嵌 CJK 字体后移除）、图例排序（降序+hex 平票）、图例列数（≤6 列，291 色单页可容纳）、导出文件名（与 PNG 同规则：豆谱-<设计名>-<W>x<H>.pdf，空名「未命名设计」，非法字符清洗）。
- `src/lib/export/pdf.ts`：pdf-lib 生成（依赖已确认安装 ^1.17.1）；图纸页（外框/单元格填充+0.35pt 边框/4pt 色号居中/板缝粗线/页眉）、图例页（标题+总数+6 列图例清单）；透明/外部格不绘制；空图纸抛错。
- `src/components/export/PdfExportButton.tsx`：页数预览确认对话框（图纸页+清单页拆解、>10 页提示）、生成中状态、空图纸错误、失败可重试、下载文件名规则；triggerDownload 独立导出便于测试替换。
- 测试：pdfLayout 18 例（E25 分页矩阵/区间标注/板缝/E26 文件名与超长截断/E27 长名与超长色号不溢出/常量自洽）+ 按钮 jsdom 5 例（空图纸错误、页数文案、真实生成 %PDF 字节、文件名断言、取消）。
- 本票单测由父代理统一运行（本环境无测试执行权限）；E2E 项（页数断言/文本抽取）属 T20。
- Blocked by: 06

## 目标

A4 分页拼图 PDF：板缝线、页坐标、每格色号，末页图例与用量清单。

## 范围

- pdf-lib 生成：A4 纵向；每格 6 mm；每页 31 列 × 45 行（10 mm 页边距 + 页码区）；每页页眉（第 x/y 页、行列区间）、板缝粗线、每格色号（4 pt，白/黑对比）。
- 末页：完整图例 + 色号用量清单（降序：色号、色块、数量）。
- 导出前页数预览与确认（200×200 ≈ 35 页）。
- 文件名同 PNG 规则（.pdf）。
- 布局计算独立成纯函数（`computePdfLayout`），供单测与 pdf-lib 绘制分离。

## 不含

- 单页缩略模式（tiled 覆盖 ≤31×45 图纸时自然为 1 页）。

## 规格引用

- spec §F7 PDF 部分；边界 E25、E26、E27。

## 验收标准

- [ ] 单测：分页计算（1×1 / 31×45 / 200×200 / 极宽图）、行列区间标注、清单排序。
- [ ] E2E：200×200 导出页数 = ceil(W/31)×ceil(H/45)+1；文本抽取抽查色号/数量正确；小图（≤31×45）为 2 页（图纸+清单）。
- [ ] 长设计名/超长自定义色号排版不溢出（断言无文本越界坐标）。

## 完成记录

（resolve 时填写）
